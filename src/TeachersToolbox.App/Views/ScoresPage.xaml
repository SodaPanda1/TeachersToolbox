<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TeachersToolbox.App.Views.ScoresPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="28" FontWeight="Bold" Text="成绩管理" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12">
            <ComboBox x:Name="ClassComboBox" Width="150" Header="班级">
                <ComboBoxItem Content="高一(1)班" />
                <ComboBoxItem Content="高一(2)班" />
                <ComboBoxItem Content="高一(3)班" />
            </ComboBox>
            <ComboBox x:Name="SubjectComboBox" Width="120" Header="科目">
                <ComboBoxItem Content="语文" />
                <ComboBoxItem Content="数学" />
                <ComboBoxItem Content="英语" />
                <ComboBoxItem Content="物理" />
                <ComboBoxItem Content="化学" />
            </ComboBox>
            <TextBox x:Name="ExamNameBox" Width="150" Header="考试名称" PlaceholderText="如：月考一" />
            <Button x:Name="LoadButton" Click="LoadButton_Click" Margin="0,20,0,0">
                加载名单
            </Button>
            <Button x:Name="SaveButton" Click="SaveButton_Click" Margin="0,20,0,0"
                    Style="{StaticResource AccentButtonStyle}">
                保存成绩
            </Button>
            <Button x:Name="AnalyzeButton" Click="AnalyzeButton_Click" Margin="0,20,0,0">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="14" Glyph="&#xE7C1;" />
                    <TextBlock Text="分析" />
                </StackPanel>
            </Button>
        </StackPanel>

        <ListView Grid.Row="2" x:Name="ScoreListView">
            <ListView.Header>
                <Grid Padding="8,12" ColumnSpacing="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60" />
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="100" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" FontWeight="Medium" Text="座号" />
                    <TextBlock Grid.Column="1" FontWeight="Medium" Text="姓名" />
                    <TextBlock Grid.Column="2" FontWeight="Medium" Text="成绩" />
                    <TextBlock Grid.Column="3" FontWeight="Medium" Text="排名" />
                    <TextBlock Grid.Column="4" FontWeight="Medium" Text="备注" />
                </Grid>
            </ListView.Header>
        </ListView>
    </Grid>
</Page>
