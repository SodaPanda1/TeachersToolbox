<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TeachersToolbox.App.Views.AssignmentsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="28" FontWeight="Bold" Text="作业管理" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12">
            <ComboBox x:Name="ClassComboBox" Width="200" Header="选择班级">
                <ComboBoxItem Content="高一(1)班" />
                <ComboBoxItem Content="高一(2)班" />
                <ComboBoxItem Content="高一(3)班" />
            </ComboBox>
            <ComboBox x:Name="SubjectComboBox" Width="150" Header="科目">
                <ComboBoxItem Content="语文" />
                <ComboBoxItem Content="数学" />
                <ComboBoxItem Content="英语" />
                <ComboBoxItem Content="物理" />
            </ComboBox>
            <Button x:Name="AddButton" Click="AddButton_Click" Margin="0,20,0,0"
                    Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="14" Glyph="&#xE710;" />
                    <TextBlock Text="布置作业" />
                </StackPanel>
            </Button>
        </StackPanel>

        <ListView Grid.Row="2" x:Name="AssignmentListView" IsItemClickEnabled="True"
                  ItemClick="AssignmentListView_ItemClick">
        </ListView>
    </Grid>
</Page>
